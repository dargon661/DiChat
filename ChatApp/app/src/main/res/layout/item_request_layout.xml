<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="5dp"
    android:background="@drawable/edit_text_background"
    android:elevation="2dp">

    <!-- Profile Image -->
    <com.google.android.material.imageview.ShapeableImageView
        android:id="@+id/IRLprofile_image"
        android:layout_width="50dp"
        android:layout_height="50dp"
        android:layout_margin="10dp"
        android:scaleType="centerCrop"
        android:src="@drawable/default_profile_image"
        app:shapeAppearance="@style/CirculaImageViewStyle" />

    <!-- Container for User Info and Button -->
    <LinearLayout
        android:layout_marginTop="13dp"

        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:layout_toEndOf="@id/IRLprofile_image"
        android:layout_marginStart="5dp"
        android:orientation="horizontal">

        <!-- User Info -->

        <!-- Button -->
        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="3"
            android:orientation="vertical">

            <TextView
                android:id="@+id/IRLuser"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginStart="10dp"
                android:textSize="20sp"
                android:textStyle="bold" />

            <TextView
                android:id="@+id/IRLPhoneNumber"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"

                android:layout_marginStart="5dp"
                android:textSize="15sp" />
        </LinearLayout>

        <Button
            android:id="@+id/IRLsend"
            android:layout_width="wrap_content"
            android:layout_height="45dp"
            android:layout_marginEnd="3dp"

            android:backgroundTint="@color/blue"


            android:text="Add"
            app:strokeColor="#0B0B0B" />

        <Button
            android:id="@+id/IRLDelete"
            android:layout_width="wrap_content"
            android:layout_height="45dp"
            android:layout_marginEnd="10dp"

            android:backgroundTint="@color/gray2"
            android:text="Delete"

            android:textColor="@color/black"
            app:strokeColor="#0B0B0B" />

    </LinearLayout>
</RelativeLayout>
